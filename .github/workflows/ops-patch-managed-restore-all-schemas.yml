name: Patch managed restore schema scope

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/ops-patch-managed-restore-all-schemas.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: fix/phase10-managed-restore-all-schemas
          fetch-depth: 0

      - name: Expand managed restore scope and remove bridge
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          path = Path('.github/workflows/phase10-managed-supabase-restore.yml')
          text = path.read_text()
          old = '''                --schema=public \\
                --exclude-table=public.spatial_ref_sys \\
'''
          new = '''                --schema=platform \\
                --schema=account \\
                --schema=authz \\
                --schema=provider \\
                --schema=catalog \\
                --schema=evidence \\
                --schema=verification \\
                --schema=discovery \\
                --schema=provider_workspace \\
                --schema=operations \\
                --schema=interaction \\
                --schema=commercial \\
                --schema=security \\
                --table=public.direkt_schema_migrations \\
'''
          if old not in text:
              raise SystemExit('expected public-only dump scope not found')
          text = text.replace(old, new, 1)
          text = text.replace(
              "jq -e '.migration_count >= 34 and .private_bucket_count == 4 and .storage_object_count == 0' artifacts/phase10-managed-restore/live-metadata.json >/dev/null",
              "jq -e '.application_schema_count == 13 and .application_table_count > 0 and .migration_count >= 34 and .private_bucket_count == 4 and .storage_object_count == 0' artifacts/phase10-managed-restore/live-metadata.json >/dev/null",
              1,
          )
          text = text.replace(
              ".public_table_count == $live[0].public_table_count\n            and .migration_count == $live[0].migration_count",
              ".application_table_count == $live[0].application_table_count\n            and .application_schema_count == $live[0].application_schema_count\n            and .migration_count == $live[0].migration_count\n            and .seed_state == $live[0].seed_state",
              1,
          )
          text = text.replace(
              'echo "- Scope: managed public application schema plus independent storage metadata verification"',
              'echo "- Scope: all 13 DIREKT application schemas plus the migration ledger; Supabase platform schemas excluded"',
              1,
          )
          path.write_text(text)
          PY
          rm .github/workflows/ops-patch-managed-restore-all-schemas.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/workflows/phase10-managed-supabase-restore.yml .github/workflows/ops-patch-managed-restore-all-schemas.yml
          git commit -m "fix(recovery): restore all DIREKT application schemas"
          git push origin HEAD:fix/phase10-managed-restore-all-schemas
