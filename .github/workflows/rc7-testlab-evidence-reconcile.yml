name: RC7 Test Lab evidence source reconciler

on:
  pull_request:
    paths:
      - ".github/workflows/rc7-testlab-evidence-reconcile.yml"

permissions:
  contents: write

jobs:
  reconcile:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.head_ref == 'fix/rc7-testlab-failure-evidence'
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
        with:
          ref: fix/rc7-testlab-failure-evidence
          fetch-depth: 0
      - name: Reconcile permanent RC7 Android failure evidence
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path: str, old: str, new: str) -> None:
              target = Path(path)
              text = target.read_text(encoding='utf-8')
              if new in text:
                  return
              if old not in text:
                  raise SystemExit(f'missing expected source in {path}')
              target.write_text(text.replace(old, new, 1), encoding='utf-8')

          test_path = Path('android/direkt-app/app/src/androidTest/java/com/kudzimusar/direkt/Rc7MapsRuntimeTest.kt')
          test_path.write_text('''package com.kudzimusar.direkt

import androidx.compose.ui.test.assertIsDisplayed
import androidx.compose.ui.test.junit4.createAndroidComposeRule
import androidx.compose.ui.test.onAllNodesWithTag
import androidx.compose.ui.test.onNodeWithTag
import androidx.compose.ui.test.onNodeWithText
import androidx.compose.ui.test.performClick
import androidx.compose.ui.test.performScrollTo
import org.junit.Assert.assertTrue
import org.junit.Rule
import org.junit.Test

class Rc7MapsRuntimeTest {
    @get:Rule
    val composeRule = createAndroidComposeRule<MainActivity>()

    @Test
    fun mapViewPreservesPrivacySafeFallbackOrLoadsRestrictedRuntime() {
        composeRule.onNodeWithText("Discover").performClick()
        composeRule.onNodeWithText("Map").performScrollTo().performClick()
        composeRule.onNodeWithTag("discovery-map-card").performScrollTo().assertIsDisplayed()
        composeRule.onNodeWithText(
            "Private provider bases never become markers.",
            substring = true,
        ).assertIsDisplayed()

        if (BuildConfig.DIREKT_MAPS_ENABLED) {
            composeRule.onNodeWithTag("discovery-google-map").assertIsDisplayed()
            composeRule.waitUntil(timeoutMillis = 25_000) {
                composeRule.onAllNodesWithTag("discovery-map-ready").fetchSemanticsNodes().isNotEmpty() ||
                    composeRule.onAllNodesWithTag("discovery-map-fallback").fetchSemanticsNodes().isNotEmpty()
            }
            val readyCount = composeRule.onAllNodesWithTag("discovery-map-ready").fetchSemanticsNodes().size
            val fallbackCount = composeRule.onAllNodesWithTag("discovery-map-fallback").fetchSemanticsNodes().size
            val loadingCount = composeRule.onAllNodesWithTag("discovery-map-loading").fetchSemanticsNodes().size
            assertTrue(
                "RC7 Maps runtime did not reach Ready; ready=$readyCount fallback=$fallbackCount loading=$loadingCount",
                readyCount > 0,
            )
            composeRule.onNodeWithTag("discovery-map-ready").assertIsDisplayed()
            assertTrue(
                composeRule.onAllNodesWithTag("discovery-map-fallback")
                    .fetchSemanticsNodes()
                    .isEmpty(),
            )
        } else {
            composeRule.onNodeWithTag("discovery-map-fallback").assertIsDisplayed()
            composeRule.onNodeWithText(
                "Manual area and list discovery remain fully available without location permission.",
            ).assertIsDisplayed()
        }
    }
}
''', encoding='utf-8')

          replace_once(
              'android/direkt-app/app/src/main/java/com/kudzimusar/direkt/ui/discovery/PrivacySafeMapCard.kt',
              '''modifier = Modifier
                                .align(Alignment.TopCenter)
                                .padding(12.dp),''',
              '''modifier = Modifier
                                .align(Alignment.TopCenter)
                                .padding(12.dp)
                                .testTag("discovery-map-loading"),''',
          )

          old_testlab = '''app_apk="android/direkt-app/app/build/outputs/apk/debug/app-debug.apk"
test_apk="android/direkt-app/app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk"
test -f "${app_apk}"
test -f "${test_apk}"
gcloud firebase test android run \\
  --project "${TESTLAB_PROJECT_ID}" \\
  --type instrumentation \\
  --app "${app_apk}" \\
  --test "${test_apk}" \\
  --device model=MediumPhone.arm,version=36,locale=en,orientation=portrait \\
  --test-targets "class ${ANDROID_TEST_CLASS}" \\
  --timeout 5m \\
  --num-flaky-test-attempts 0 \\
  --no-use-orchestrator \\
  --no-record-video \\
  --no-performance-metrics \\
  --no-auto-google-login \\
  --client-details matrixLabel="DIREKT RC7 Maps ${SOURCE_SHA}" \\
  --format=json > "${RUNNER_TEMP}/rc7-maps-test-lab.json"
receipt "android_test_lab_map_ready=PASS"
receipt "android_test_device=MediumPhone.arm_api36"
receipt "android_flaky_retries=0"
receipt "android_location_permission_requested=false"
receipt "manual_list_fallback_preserved=true"
'''
          new_testlab = '''app_apk="android/direkt-app/app/build/outputs/apk/debug/app-debug.apk"
test_apk="android/direkt-app/app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk"
test -f "${app_apk}"
test -f "${test_apk}"
apksigner_bin="$(find "${ANDROID_HOME:-${ANDROID_SDK_ROOT:-}}/build-tools" -type f -name apksigner | sort -V | tail -n 1)"
test -x "${apksigner_bin}"
apk_certificate_sha1="$("${apksigner_bin}" verify --print-certs "${app_apk}" \\
  | awk -F': ' '/Signer #1 certificate SHA-1 digest:/{print $2; exit}' \\
  | tr -d ':' \\
  | tr '[:lower:]' '[:upper:]')"
[[ "${apk_certificate_sha1}" =~ ^[0-9A-F]{40}$ ]]
test "${apk_certificate_sha1}" = "${android_sha1^^}"
jq -n \\
  --arg packageName "${ANDROID_PACKAGE}" \\
  --arg certificateSha1 "${apk_certificate_sha1}" \\
  '{packageName: $packageName, certificateSha1: $certificateSha1, matchesRestrictedKey: true}' \\
  > "${RUNNER_TEMP}/rc7-android-apk-certificate.json"
receipt "android_apk_certificate_sha1=${apk_certificate_sha1}"
receipt "android_apk_certificate_matches_key=true"

testlab_stderr="${RUNNER_TEMP}/rc7-maps-test-lab.stderr"
testlab_failure="${RUNNER_TEMP}/rc7-maps-test-lab-failure.json"
set +e
gcloud firebase test android run \\
  --project "${TESTLAB_PROJECT_ID}" \\
  --type instrumentation \\
  --app "${app_apk}" \\
  --test "${test_apk}" \\
  --device model=MediumPhone.arm,version=36,locale=en,orientation=portrait \\
  --test-targets "class ${ANDROID_TEST_CLASS}" \\
  --timeout 5m \\
  --num-flaky-test-attempts 0 \\
  --no-use-orchestrator \\
  --no-record-video \\
  --no-performance-metrics \\
  --no-auto-google-login \\
  --client-details matrixLabel="DIREKT RC7 Maps ${SOURCE_SHA}" \\
  --format=json > "${RUNNER_TEMP}/rc7-maps-test-lab.json" 2> "${testlab_stderr}"
testlab_code=$?
set -e
if [[ "${testlab_code}" -ne 0 ]]; then
  matrix_id="$(sed -n 's/.*Test \[\(matrix-[^]]*\)\].*/\1/p' "${testlab_stderr}" | tail -n 1)"
  receipt "android_test_lab_map_ready=FAILED"
  receipt "android_test_lab_matrix_id=${matrix_id:-unavailable}"
  if [[ -n "${matrix_id}" ]]; then
    python3 scripts/rc7/collect-testlab-failure.py \\
      --project "${TESTLAB_PROJECT_ID}" \\
      --matrix "${matrix_id}" \\
      --output "${testlab_failure}"
    receipt "android_test_lab_failure_evidence_present=true"
  else
    jq -n '{schema: "direkt.rc7.testlab-failure.v1", rawLogsIncluded: false, credentialIncluded: false, apiKeyIncluded: false, coordinateValuesIncluded: false, participantDataIncluded: false, matrixId: "unavailable", diagnostic: "matrix_id_unavailable"}' > "${testlab_failure}"
    receipt "android_test_lab_failure_evidence_present=false"
  fi
  cat "${testlab_failure}" >&2
  exit "${testlab_code}"
fi
rm -f "${testlab_stderr}"
receipt "android_test_lab_map_ready=PASS"
receipt "android_test_device=MediumPhone.arm_api36"
receipt "android_flaky_retries=0"
receipt "android_location_permission_requested=false"
receipt "manual_list_fallback_preserved=true"
'''
          replace_once('scripts/rc7/run-maps-managed.sh', old_testlab, new_testlab)

          verifier = 'scripts/rc7/verify-maps-contract.py'
          replace_once(verifier, '"waitUntil(timeoutMillis = 20_000)",', '"waitUntil(timeoutMillis = 25_000)",')
          replace_once(
              verifier,
              '        "discovery-map-fallback",\n        "fetchSemanticsNodes()",',
              '        "discovery-map-fallback",\n        "discovery-map-loading",\n        "RC7 Maps runtime did not reach Ready",\n        "fetchSemanticsNodes()",',
          )
          replace_once(
              verifier,
              '        "--num-flaky-test-attempts 0",\n        "cleanup.cloud_run_job_deleted",',
              '        "--num-flaky-test-attempts 0",\n        "apksigner",\n        "android_apk_certificate_matches_key=true",\n        "collect-testlab-failure.py",\n        "android_test_lab_failure_evidence_present=true",\n        "cleanup.cloud_run_job_deleted",',
          )

          context = 'scripts/rc7/verify-managed-workflow-context.py'
          replace_once(
              context,
              'MANAGED_SCRIPT = ROOT / "scripts/rc7/run-maps-managed.sh"\nGEOCODING_PORT',
              'MANAGED_SCRIPT = ROOT / "scripts/rc7/run-maps-managed.sh"\nTESTLAB_COLLECTOR = ROOT / "scripts/rc7/collect-testlab-failure.py"\nGEOCODING_PORT',
          )
          replace_once(
              context,
              '    managed_script = MANAGED_SCRIPT.read_text(encoding="utf-8")\n    geocoding_port',
              '    managed_script = MANAGED_SCRIPT.read_text(encoding="utf-8")\n    testlab_collector = TESTLAB_COLLECTOR.read_text(encoding="utf-8")\n    geocoding_port',
          )
          replace_once(
              context,
              '    failure_artifact = "${{ runner.temp }}/rc7-maps-canary-failure.json"\n    require_once(workflow, failure_artifact, "RC7 must upload the sanitized canary failure artifact.")',
              '    failure_artifact = "${{ runner.temp }}/rc7-maps-canary-failure.json"\n    require_once(workflow, failure_artifact, "RC7 must upload the sanitized canary failure artifact.")\n    testlab_failure_artifact = "${{ runner.temp }}/rc7-maps-test-lab-failure.json"\n    require_once(workflow, testlab_failure_artifact, "RC7 must upload sanitized Test Lab failure evidence.")\n    apk_certificate_artifact = "${{ runner.temp }}/rc7-android-apk-certificate.json"\n    require_once(workflow, apk_certificate_artifact, "RC7 must upload the final APK certificate receipt.")',
          )
          replace_once(
              context,
              '    prohibit(managed_script, r\'cat "\\$\\{execution_details\\}"\', "Raw execution details must not be printed.")\n\n    for marker in (',
              '    prohibit(managed_script, r\'cat "\\$\\{execution_details\\}"\', "Raw execution details must not be printed.")\n\n    for marker in (\n        "testing.googleapis.com/v1/projects",\n        "toolresults.googleapis.com/toolresults/v1beta3",\n        "rawLogsIncluded",\n        "credentialIncluded",\n        "apiKeyIncluded",\n        "coordinateValuesIncluded",\n        "participantDataIncluded",\n        "stackTraces",\n    ):\n        require_present(testlab_collector, marker, "Bounded Test Lab evidence collector drifted.")\n    prohibit(testlab_collector, r"toolOutputs", "Opaque Test Lab output files must not be collected.")\n\n    for marker in (',
          )
          replace_once(
              context,
              '    print("canary_failure_evidence_sanitized=true")\n    print("raw_canary_logs_uploaded=false")',
              '    print("canary_failure_evidence_sanitized=true")\n    print("testlab_failure_evidence_sanitized=true")\n    print("final_apk_certificate_verified=true")\n    print("raw_canary_logs_uploaded=false")',
          )

          status = Path('docs/integrations/CURRENT_INTEGRATION_STATUS.md')
          status_text = status.read_text(encoding='utf-8')
          old = 'Exact-main run `30225624823/1` then proved WIF, fresh `1 JPY` owner budget attestation, quota, Android key restriction, immutable image, OAuth execution and cleanup, but the canary rejected the leading ranked result as outside Zambia before Test Lab. The corrective adapter now selects only the first structurally valid candidate that independently satisfies both a `ZM` country signal and the unchanged Zambia coordinate bounds.'
          new = 'Exact-main run `30225624823/1` proved the infrastructure path but exposed ranked-result selection, which is corrected. Exact-main run `30226241329/1` then proved the backend Geocoding v4 OAuth canary, budget, quota, Android key restriction, immutable image and cleanup; the remaining failure is one API 36 instrumentation test in matrix `matrix-3gndt2ks91n33`. The next proof verifies the certificate embedded in the final APK and captures bounded Tool Results test-case evidence while continuing to require actual map Ready state.'
          if new not in status_text:
              if old not in status_text:
                  raise SystemExit('missing current-status RC7 evidence text')
              status.write_text(status_text.replace(old, new, 1), encoding='utf-8')

          ledger = Path('docs/integrations/LIVE_INTEGRATION_LEDGER.md')
          ledger_text = ledger.read_text(encoding='utf-8')
          old = 'Exact-main run `30225624823/1` proved WIF, fresh owner budget attestation, quota, key restriction, immutable image, OAuth execution and cleanup, then failed before Test Lab because result zero was outside Zambia. The corrective adapter iterates ranked results and accepts only a structurally valid candidate with an independent `ZM` country signal and coordinates inside the unchanged Zambia bounds. Artifact `8638498996`, digest `sha256:55c3b9f581ee899b5f1cac7e2a99e5d7851faedb00def2ba887e760e22a8a56a`; Cloud Run Job cleanup passed.'
          new = 'Run `30225624823/1` exposed ranked-result selection and was corrected. Exact-main run `30226241329/1` then passed the backend Geocoding v4 OAuth canary and all budget/quota/key/image controls, but one API 36 instrumentation test failed in matrix `matrix-3gndt2ks91n33`. Artifact `8638705116`, digest `sha256:49fbc387d06eeb7061ca1c8923a313c8dff22f534e2940398f7d4df26d55233c`; backend PASS and Cloud Run Job cleanup passed. The next proof verifies the final APK signing certificate against the restricted Android key and emits only bounded Tool Results test-case evidence; fallback remains a failure.'
          if new not in ledger_text:
              if old not in ledger_text:
                  raise SystemExit('missing ledger RC7 evidence text')
              ledger.write_text(ledger_text.replace(old, new, 1), encoding='utf-8')

          notes = Path('docs/integrations/RC7_GOOGLE_MAPS_IMPLEMENTATION_NOTES.md')
          notes_text = notes.read_text(encoding='utf-8')
          marker = '## Credential and authentication boundary\n'
          section = '''## Android Test Lab evidence correction\n\nExact-main run `30226241329/1` on `58c3b36d15ca8b602ed2365242c93d965a7ff08d` proved the backend service-identity OAuth Geocoding v4 canary, fresh one-JPY owner attestation, quota, Android key restriction, immutable image and cleanup. The remaining result was one failed API 36 instrumentation case in Test Lab matrix `matrix-3gndt2ks91n33`. Artifact `8638705116` has digest `sha256:49fbc387d06eeb7061ca1c8923a313c8dff22f534e2940398f7d4df26d55233c`; `cleanup.cloud_run_job_deleted=true` and `cleanup_failed=false`.\n\nThe managed proof now verifies the SHA-1 certificate embedded in the final debug APK and requires it to equal the certificate restriction applied to the synthetic Android key. When Test Lab fails, the authenticated exact-main job queries the Testing and Tool Results APIs and writes a whitelisted receipt containing matrix state, step outcome, test-case identity and bounded stack traces. Raw logs, opaque tool outputs, credentials, API-key values, coordinates and participant data remain excluded. The instrumentation assertion still requires `discovery-map-ready`; a privacy-safe fallback is diagnostic evidence, not a pass.\n\n'''
          if section not in notes_text:
              if marker not in notes_text:
                  raise SystemExit('missing implementation notes marker')
              notes.write_text(notes_text.replace(marker, section + marker, 1), encoding='utf-8')
          PY

          python3 -m py_compile scripts/rc7/collect-testlab-failure.py
          bash -n scripts/rc7/run-maps-managed.sh
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          if git diff --quiet; then
            exit 0
          fi
          git add android/direkt-app/app/src/androidTest/java/com/kudzimusar/direkt/Rc7MapsRuntimeTest.kt \
            android/direkt-app/app/src/main/java/com/kudzimusar/direkt/ui/discovery/PrivacySafeMapCard.kt \
            scripts/rc7/run-maps-managed.sh \
            scripts/rc7/verify-maps-contract.py \
            scripts/rc7/verify-managed-workflow-context.py \
            docs/integrations/CURRENT_INTEGRATION_STATUS.md \
            docs/integrations/LIVE_INTEGRATION_LEDGER.md \
            docs/integrations/RC7_GOOGLE_MAPS_IMPLEMENTATION_NOTES.md
          git commit -m 'fix(rc7): preserve bounded Test Lab failure evidence'
          git push origin HEAD:fix/rc7-testlab-failure-evidence
