name: Phase 10 staging typed-fix formatting

on:
  push:
    branches:
      - build/android-v1
    paths:
      - ".github/workflows/phase10-staging-type-format.yml"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: build/android-v1
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: admin/direkt-operations-portal/package-lock.json
      - working-directory: admin/direkt-operations-portal
        run: npm ci --ignore-scripts
      - working-directory: admin/direkt-operations-portal
        run: npx prettier --write src/lib/operations-api.ts src/app/api/health/route.ts
      - run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add admin/direkt-operations-portal/src/lib/operations-api.ts \
            admin/direkt-operations-portal/src/app/api/health/route.ts
          git diff --cached --quiet && exit 0
          git commit -m "style(staging): format typed portal fixes"
          git push origin HEAD:build/android-v1
