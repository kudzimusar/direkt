# RC3 Crashlytics Managed Canary Authorization

**Purpose:** one-shot source-controlled authorization marker for the RC3 Firebase Crashlytics managed synthetic canary.

**Authorized checkpoint:** RC3 — Firebase Crashlytics Android runtime closure.

**Exact merged source under proof:** `a951fe6c6e41c0903643c956ac2268f7cdcf6cb3`.

**Authorization source:** owner instruction to continue the remaining integration programme non-stop while preserving documented regression and managed-evidence controls.

**Boundary:** synthetic-only debug crash and ANR canary on the registered DIREKT Firebase debug application. This does not authorize participant/production crash telemetry, Firebase Analytics, FCM, Test Lab, production AI, real communications, real payments, Phase 11 exit or Phase 12 production release.

**Required control token:** `RUN-DIREKT-CRASHLYTICS-CANARY`

**Dispatch marker:** marker-only PR synchronize/open event on `integration/rc3-crashlytics-managed-run`; the audited bridge must dispatch the canary against the PR base SHA and that SHA must equal the exact merged source above.

This marker is temporary and must be removed after the RC3 managed proof is recorded and the checkpoint is formally closed.
